// Business Logic
function Place(location, landmark, season, notes, tags) {
  this.placeLocatopn = location;
  this.placelandmark = landmak;
  this.placeSeason = season;
  this.placeNotes = notes;
  this.place.Tags = tags;
}

// user interface logic
